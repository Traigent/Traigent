# Walkthrough Utilities

Shared helpers for the JS walkthrough examples:

- `helpers.mjs` - dataset loading and result printing
- `scoring.mjs` - exact-match / semantic / classification / code heuristics
- `mock_answers.mjs` - deterministic mock behavior for offline walkthrough runs
- `check_environment.mjs` - quick environment diagnostics

