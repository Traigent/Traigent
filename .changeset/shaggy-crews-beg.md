---
'@traigent/sdk': patch
---

Harden CI parity coverage by vendoring the canonical cross-SDK oracle fixtures into the repo and removing the private cross-repo checkout dependency from the parity job.
