# Testing requirements for Traigent local mode
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-asyncio>=0.21.0
coverage>=7.0.0
click>=8.0.0
requests>=2.33.0
ragas>=0.3.6
rapidfuzz>=3.14.0

# Core dependencies needed for tests
-r requirements.txt

# Analytics dependencies needed for insights tests
-r requirements-analytics.txt

# Bayesian dependencies needed for optimization tests
-r requirements-bayesian.txt

# Security dependencies needed for enterprise tests
-r requirements-security.txt

# Integration dependencies needed for framework tests
-r requirements-integrations.txt

# Development dependencies
black>=26.3.1
flake8>=5.0.0
mypy>=1.0.0
