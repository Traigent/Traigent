# Examples Walkthroughs

This section walks through the shipped examples:

- `rag-support-bot.yml`: enums, numeric domains, chance SLO, DNF implication
- `text-to-sql.yml`: enumeration domain and banded target
- `tool-use.yml`: fairness SLO blocking promotions

Configuration walkthroughs:

- `configurations/rag-support-bot.config.yml`: assignment consistent with the spec
- `measurements/rag-support-bot.measure.yml`: illustrates operational & chance checks

Run `tvl-parse`, `tvl-lint`, and `tvl-validate` on each to inspect and verify.
