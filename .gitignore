# Internal documentation (private feature planning)
docs/internal/
EXCEPTION_HANDLING_SPEC.md

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Temporary and deprecated directories
temp_testing_tutorial_as_a_user/
demo_venv/
traigent_test_env/
cache/
sections/
# But keep example site sections tracked
!examples/sections/
!examples/sections/**
traigent_schema/
# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Build artifacts for publication
*.whl
*.tar.gz
.build/
_bmad/
# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/
playground/optimization_logs
**/cache/**/*.json
**/demo_results/**/*.json
**/demo_results/**/*.csv


# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/#use-with-ide
.pdm.toml

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.env.*
!.env.example
!.env.*.template
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be added to the global gitignore or merged into this project gitignore.  For a PyChell:
#  https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
.idea/

# VS Code
# Keep .vscode settings tracked for team consistency
# .vscode/

# Internal design docs (IP/private during implementation)
resampling_configs.md
Traigent_vs_Langfuse_Gap_Analysis.xlsx
docs/feature_matrices/enterprise_beta_execution_plan.md
docs/feature_matrices/enterprise_beta_execution_tracker.md

# Traigent specific
*.jsonl
# But keep example datasets tracked for users to run examples
!examples/datasets/**/*.jsonl
# Keep use-cases datasets tracked for demo use cases
!use-cases/**/datasets/*.jsonl
# Keep walkthrough example datasets tracked
!walkthrough/datasets/*.jsonl
# Keep CI example datasets tracked
!examples/integrations/ci-cd/**/*.jsonl
# Keep the quickstart symlink at repo root for README examples
!qa_samples.jsonl
!tests/mcp/fixtures/sample_datasets.jsonl
optimization_results/
local_results/
data/
temp/
.traigent/
# Cost approval tokens (user-specific, should never be committed)
**/cost_approval.token
.playwright-cli/

# Mutation testing artifacts (mutmut)
mutants/
mutation_results.txt
setup.cfg
.mutmut-cache/
tests/mutation/

# Mutation testing scripts (development only)
scripts/mutmut_runner.sh
scripts/run_mutmut_with_setup.sh

# Temporary test scripts (development phase)
test_phase*.py
manual_validation_*.py
test_multi_objective_implementation.py
test_weighted_score_updates.py
test_integration_weighted_scores.py
analyze_my_logs.py

# Backup files
*_backup.py
*_original_backup.py

# OS specific
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Desktop.ini
*.pyc
*.gz
__pycache__/

# Additional cache and temp files
None
screenshot.png
optimization_result.json

# Database files (MLflow, test, cache)
*.db
mlflow.db
test_concurrent.db

# DSPy cache directories
.dspy_cache/
**/.dspy_cache/

# IDE specific
.benchmarks/
.agents/
.claude/
.claude/skills/
.pylance-settings.json
.flake8
.ruff_cache/

# Test/build artifacts
htmlcov/
.coveragerc

# Code reviews and internal documentation (user-specific)
code_reviews/
tmp/code-review/
CLAUDE.local.md
review_*.md
linkedin_profile_paste_ready.md

# Cleanup/fix scripts (temporary)
fix_*.py
cleanup_*.py
validate_*.py
# But keep test validation tools
!tests/optimizer_validation/tools/validate_*.py
*_REPORT.md
*_COMPLETE.md
*_TRACKER.md
*_AUTOMATION.md
*_PROTOCOL.md

# DVC infrastructure (pipeline examples are in examples/ci-pipeline/)
.dvc/
.dvcignore

# Virtual environments (additional)
test_env/

# Publication and release artifacts
.tox/
pip-audit-report.json
bandit-report.json
safety-report.json
security-reports/
quality-reports/
dependency-reports/

# Documentation build artifacts
docs/_build/
docs/.doctrees/
site/

# Release preparation
release-notes/
dist-packages/examples/sections/examples/test_results.json
# Optimization results - temporary files
results.json
**/results.json


#Ignore vscode AI rules
.github/instructions/codacy.instructions.md

# Local development folders
.traigent_local/
.traceability_venv/
test_concurrent.db
scripts/secrets/
.scannerwork/
sonar-project.local.properties

# Traigent generated results and caches
**/.traigent_results/
**/.traigent_validation_results/
**/.validation_results/
**/results-ci/
**/.config/mlflow/
REPO_AUDIT.md

# Generated example/template files
traigent_example.py

# Git archive (local backup of original history)
.git_archive/

# Reports (internal audits archived privately)
reports/

# IDE settings (may contain local paths)
.vscode/

# Backup environments
.venv.bkp/

# Test coverage sprint coordination (multi-agent test writing)
.test-coverage-sprint/

# Workspace-specific GitHub CLI config
.gh/
.envrc

# MCP server configuration (contains API tokens)
.mcp.json

# Optimizer validation generated artifacts
tests/optimizer_validation/viewer/graph_data.json
tests/optimizer_validation/viewer/_results/
tests/optimizer_validation/viewer/*.html
!tests/optimizer_validation/viewer/index.html
!tests/optimizer_validation/viewer/failed_tests.html
tests/optimizer_validation/viewer/ontology/*.ttl
!tests/optimizer_validation/viewer/ontology/schema.ttl

# Large test reports
report.json

# MLflow artifacts and database (local development)
mlruns/
mlflow.db
.secrets.*
!.secrets.baseline

# Trial output files (local development)
trial_*.json

# Agent instruction files (development only)
AGENTS.md
tests/optimizer_validation/AGENT_*.md
tests/optimizer_validation/GIST_*.md
tests/optimizer_validation/GPT_*.md
tests/optimizer_validation/*_TEMPLATE*.md
tests/optimizer_validation/TEST_REVIEW_CHECKLIST.md
/metrics/
.env
_bmad-output/
coverage.json

# Local development artifacts
tests/manual/
tmp/
walkthrough/myfiles.md
scripts/smoke/run_local_real_openrouter_quick_smoke.sh
.traigent_validation_results/
.validation_results/
results-ci/
REPO_AUDIT.md
docs/tvl/specification.pdf
docs/tvl/tvl-website/client/public/docs/specification.pdf

# Presentations (internal/sensitive)
presentations/

# Design documentation (local only)
docs/design/

# Test artifacts (generated by Playwright, pytest, allure)
artifacts/
test-results/
tests/optimizer_validation/viewer/bdd_graph_data.json

# Architecture diagrams (generated locally)
*.mmd

# Local discussion notes and experiments
discussion_*.txt
discussion_*.md
DISCUSSIONS_RECOUNT_*.md
AutoCoder_orders.txt
old_discussions_*/
paper_experiments/

# Internal/proprietary docs (relocated to Traigent/business-strategy and Traigent/internal-docs)
.archive/docs-internal/
.release_review/
.post_release_recommendation_fixes/
local/
CLAUDE.md
docs/hybrid-mode-integration-guide.pdf
examples/archive/
examples/bazak/
docs/marketing/
docs/reviews/what_if_*/
scripts/project_review/
tests/BROAD_TEST_REVIEW_*.json

# Nexxen demo resources (client-specific, not for version control)
examples/nexxen/
examples/datasets/nexxen/
examples/nexxen.zip

# Local helper scripts
walkthrough/run_with_env.sh
scripts/release/build_offline_zip.sh

# Release polish: internal tracking and dev artifacts (root-level)
israel_issues*.json
project_items.json
sonarcloud_issues*.csv
coverage_observability_modules.xml
RELIABILITY_SWEEP_*.md
PR[0-9]*.md
playground.py
playground/
my_llm_project/
test_redact*.py
test_timeout*.py
nvidia/

# LaTeX build artifacts
*.aux
*.out

# Internal maintenance and dev-only directories
.archive/
.post_release_recommendation_fixes/
.release_review/
.codesync.yml
MAINTENANCE.md
/tools/
scripts/project_review/
docs/installation-guide-offline.md

# Manual validation harnesses (manual_validation/) — two halves, both needed.
#
# Half 1 (ignore everything by default): the harnesses are run by hand and
# leave artifacts next to themselves — __pycache__/, .pyc, run logs, and
# dataset/result dumps. None of that belongs in the repo. This is also what the
# original blanket `manual_validation/` rule (#416) was for: back then the
# directory was purely a dump of internal dev scratch scripts, untracked with
# `git rm --cached` so they stayed on contributors' disks.
#
# Half 2 (un-ignore the tracked harnesses): the directory now has a tracked,
# documented purpose (see manual_validation/README.md) — the harnesses and
# their README are committed on purpose, and ignoring them outright made
# `git add manual_validation/` drop a new harness silently.
#
# The negation is deliberately narrow: the top level only (never `**`, so no
# subdirectory is re-included), and only the `*_check.py` harness naming
# convention plus the README. Anything broader re-includes at higher priority
# than the *earlier* global rules in this file and quietly un-ignores things
# they exist to protect — a `!manual_validation/**/*.py` + `!.../*.md` pair
# overrode `local/` (:445), `local_settings.py` (:84), `local_results/` (:212),
# `test_phase*.py` (:232) and `manual_validation_*.py` (:233), i.e. the exact
# internal scratch scripts the relocation in 892456cc moved under `local/` to
# keep out. `*_check.py` and `README.md` collide with no un-anchored global
# rule in this file, so this negation is safe by construction rather than by a
# hand-maintained mirror of every rule above.
#
# Adding a harness: name it `<subject>_check.py`. Anything else here is treated
# as a run artifact; use `git add -f` if you genuinely need to track one.
manual_validation/**
# Belt and braces: keep the never-commit families ignored here even if the
# negations below are ever loosened. These sit *before* the negations on
# purpose. Last match wins in git, so listing them after would make them beat
# `!manual_validation/*_check.py` — and `test_*.py` would then silently drop a
# harness whose subject happens to start with "test" (`test_runner_check.py`),
# which is the exact silent-drop failure this whole block exists to prevent.
# Ordering them first still serves their stated purpose: they only need to lose
# to an explicit `*_check.py`/`README.md` negation, not to win over it.
manual_validation/**/__pycache__/
manual_validation/**/local/
manual_validation/**/local_results/
manual_validation/**/local_settings.py
manual_validation/**/test_*.py
manual_validation/**/manual_validation_*.py
!manual_validation/README.md
!manual_validation/*_check.py
